#include "pch.h"
#include "InterpreterTestDefs.h"
#include "Eval.h"


namespace InterpreterTest
{
	TEST(TestEval, TestCorrectEval)
	{
		/*LEAF(l1, VAR("x"));
		LEAF(l2, VAR("y"));
		BRANCH(root, OP("+"), l1, l2);
		EVAL(eval, root);

		EXPECT_TRUE(FLOAT_EQ(eval(1.0f, 1.0f), 2.0f));*/
	}
}